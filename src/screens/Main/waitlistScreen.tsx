import { Text, View } from 'react-native';

const WaitlistScreen = () => {
    return (
        <View>
            <Text>waitlistScreen</Text>
        </View>
    );
};

export default WaitlistScreen;
